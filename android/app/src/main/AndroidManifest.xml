<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Phase 1 permissions - exactly what this phase's real functionality uses, nothing from
        later phases (no location permissions yet; those are Phase 3).

        SCHEDULE_EXACT_ALARM: lets the app request exact-alarm scheduling on Android 12+ (the
            user must still grant it via Settings.ACTION_REQUEST_SCHEDULE_EXACT_ALARM, handled by
            MainActivity's permission banner).
        RECEIVE_BOOT_COMPLETED: lets BootReceiver reschedule alarms from Room after a reboot.
        POST_NOTIFICATIONS: Android 13+ runtime permission for showing the ringing notification.
        WAKE_LOCK: keeps the CPU awake long enough for AlarmReceiver's coroutine and the ringing
            notification/activity to start when the device is asleep.
        USE_FULL_SCREEN_INTENT: required to show AlarmRingingActivity over the lock screen via a
            full-screen notification intent.
        VIBRATE: alarm vibration pattern in AlarmRingingActivity.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".SmartAlarmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartAlarm">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartAlarm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Lock-screen visibility (setShowWhenLocked/setTurnScreenOn on API 27+, or the
            equivalent WindowManager flags below API 27) is set in code in
            AlarmRingingActivity.onCreate() - there is no manifest attribute for it.
        -->
        <activity
            android:name=".ui.ringing.AlarmRingingActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.SmartAlarm" />

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.AlarmActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
