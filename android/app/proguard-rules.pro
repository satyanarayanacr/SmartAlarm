# Add project specific ProGuard rules here.
# Empty-app foundation: minifyEnabled is currently false for the release build type
# (see app/build.gradle.kts), so these rules are not yet exercised. Populate this
# file with Room / Gson / reflection-based keep rules once Phase 1 persistence and
# any serialization is introduced, then enable minification for release builds.

# Uncomment to preserve line numbers for stack traces in production crash reports:
#-keepattributes SourceFile,LineNumberTable
#-renamesourcefileattribute SourceFile
